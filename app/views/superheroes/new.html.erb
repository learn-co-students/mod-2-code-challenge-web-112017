<%= form_for :superhero do |f| %>
  Name: <%= f.text_field :name %><br />
  Superhero name : <%= f.text_field :super_name %><br />
  Superpower : <%= f.collection_select :superpower_id, @superpowers, :id, :name %><br />
  <%= f.submit %>
<% end %>

<!-- add in flash error messages if any -->
