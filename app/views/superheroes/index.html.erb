<h2>All Superheroes</h2>

<ul>
  <% @superheroes.each do |superhero| %>
    <li>
      <%= link_to superhero.super_name, superhero %>
    </li>
    <br>
  <% end %>
</ul>


#Tried adding the search feature in the last few minutes and I think it broke everything?!
<%= form_tag(superheroes_path, :method => "get", id: "search-form") do %>
    <%= text_field_tag :search, params[:search], placeholder: "Search Superpowers" %>
    <%= submit_tag "Search", :name => nil %>
<% end %>
